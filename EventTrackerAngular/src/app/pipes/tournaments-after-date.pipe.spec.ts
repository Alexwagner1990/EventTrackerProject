import { TournamentsAfterDatePipe } from './tournaments-after-date.pipe';

describe('TournamentsAfterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TournamentsAfterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
